#include "holberton.h"

/**
 * set_string - changing the values of chars
 * @s: source matrix
 * @to: change it to
 * Return: the new value of s.
 */
void set_string(char **s, char *to)
{
	*s = to;
}
