#include "holberton.h"

/**
 * wildcmp - check if a scring is palindrome
 * @s: string to check
 * Return: 0 if not 1 if yes.
 */
int wildcmp(char *s1, char *s2)
{
	if (s1 > s2)
	{
		return (0);
	}
	return (1);
}
